<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>Edit your chat <%=chat.id%></h3>
    <p>THis chat was sent from the <b><%= chat.from %></b> to <b><%= chat.to%></b></p>
    <form action="/chats/<%=chat.id%>?_method=PUT" method="POST">
        <textarea name="msg" id="" cols="30" rows="10"><%= chat.msg%></textarea>
        <button>Edit</button>
    </form>
</body>
</html>